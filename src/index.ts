// toto-react — client-side barrel
//
// Re-export all shared React components, hooks, contexts, and API clients here.
// Example:
//   export { TotoContext, useToto } from './context/TotoContext';
//   export { default as TotoButton } from './components/TotoButton';
//   export { default as useTotoApi } from './hooks/useTotoApi';
