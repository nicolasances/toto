// toto-react — server-side barrel (Next.js API route handlers)
//
// Re-export Next.js API route handlers that consuming apps re-export inside
// their own `app/api/` routes. Example:
//   export { default as sttHandler } from './api/stt';
//   export { default as ttsHandler } from './api/tts';
